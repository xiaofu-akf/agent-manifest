{
  "name": "@openconductor/agent-manifest",
  "version": "0.1.0",
  "description": "Portable, permission-aware manifests for agents, skills, templates and workflows.",
  "type": "module",
  "license": "MIT",
  "bin": { "oc-manifest": "./bin/validate.mjs" },
  "scripts": { "test": "node --test", "validate": "node ./bin/validate.mjs" },
  "engines": { "node": ">=20" }
}
